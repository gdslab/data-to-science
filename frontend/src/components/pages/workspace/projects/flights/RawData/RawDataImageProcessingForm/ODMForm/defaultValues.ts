import { ODMSettings } from '../../RawData.types';

const defaultValues: ODMSettings = {
  backend: 'odm',
  orthoResolution: 4,
  pcQuality: 'medium',
  disclaimer: false,
};

export default defaultValues;
